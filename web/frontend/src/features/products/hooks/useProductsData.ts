import { useProducts } from "@/hooks/useProducts";

/**
 * Feature-specific hook for products data
 * Can add additional logic specific to products feature here
 */
export const useProductsData = useProducts;
